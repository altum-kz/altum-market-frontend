import { OTPConfirmFormView } from "@/widgets/auth/otp";

export const OtpPage = () => {
    return (
        <main className="flex min-h-screen items-center justify-center bg-gray-50">
            <OTPConfirmFormView />
        </main>
    );
};