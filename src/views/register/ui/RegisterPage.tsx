import {RegisterFormView} from "@/widgets/auth/register";

export const RegisterPage = () => {
    return (
        <main className="flex flex-1 items-center justify-center bg-gray-50">
            <RegisterFormView />
        </main>
    );
};